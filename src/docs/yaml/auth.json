{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "refresh": {
      "id": "/api/auth/refresh",
      "title": "Refresh",
      "description": "Refresh the JWT.",
      "type": "object",
      "properties": {
        "refresh": {
          "description": "Refresh token",
          "type": "string"
        }
      },
      "required": [
        "refresh"
      ]
    }
  }
}